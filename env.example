# ============================================
# WhaTicket - Docker Compose Environment Variables
# ============================================
# Copie este arquivo para .env e configure as variáveis conforme necessário
# cp env.example .env

# ============================================
# MYSQL DATABASE CONFIGURATION
# ============================================
MYSQL_ENGINE=mariadb                    # Engine do banco (mariadb ou mysql)
MYSQL_VERSION=10.6                      # Versão do MySQL/MariaDB
MYSQL_ROOT_PASSWORD=strongpassword      # ⚠️ ALTERE ESTA SENHA!
MYSQL_DATABASE=whaticket                # Nome do banco de dados
MYSQL_PORT=3306                         # Porta para expor o MySQL (padrão: 3306)
TZ=America/Fortaleza                    # Timezone (padrão: America/Fortaleza)

# ============================================
# BACKEND CONFIGURATION
# ============================================
BACKEND_PORT=8080                       # Porta para expor o backend (padrão: 8080)
BACKEND_SERVER_NAME=                    # Nome do servidor backend (ex: api.mydomain.com) - opcional
BACKEND_URL=http://localhost            # URL do backend (use https:// em produção)
PROXY_PORT=8080                         # Porta do proxy (padrão: 8080, use 443 em produção com SSL)

# JWT SECRETS - ⚠️ ALTERE ESTES VALORES!
JWT_SECRET=3123123213123                # Secret para JWT tokens
JWT_REFRESH_SECRET=75756756756          # Secret para refresh tokens

# ============================================
# FRONTEND CONFIGURATION
# ============================================
FRONTEND_PORT=3000                      # Porta HTTP do frontend (padrão: 3000, use 80 em produção)
FRONTEND_SSL_PORT=3001                  # Porta HTTPS do frontend (padrão: 3001, use 443 em produção)
FRONTEND_SERVER_NAME=                   # Nome do servidor frontend (ex: myapp.mydomain.com) - opcional
FRONTEND_URL=http://localhost:3000     # URL do frontend (use https:// em produção)

# ============================================
# SSL CERTIFICATES (Opcional)
# ============================================
# Para usar SSL, coloque os certificados em:
# ./ssl/certs/backend/fullchain.pem
# ./ssl/certs/backend/privkey.pem
# ./ssl/certs/frontend/fullchain.pem
# ./ssl/certs/frontend/privkey.pem

# ============================================
# BROWSERLESS (Opcional)
# ============================================
# MAX_CONCURRENT_SESSIONS=1             # Use apenas se estiver usando browserless

